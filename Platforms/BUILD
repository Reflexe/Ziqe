load('//BuildTools:platform_settings.bzl', 'select_platform_target', 'add_platform_config_settings')

package(default_visibility = ["//visibility:public"])

#cc_library (
        #name="CppCore",
        #deps = select_platform_target(
                #targets=['CppCore'],
                #not_platform_targets=['//Platforms/Common:CppCore']
               #)
#)

#cc_library (
        #name="OS",
        #deps = select_platform_target(
                #targets=['OS'],
                #not_platform_targets=['//Platforms:CppCore']
               #),
#)


#cc_library (
        #name="PerDriver",
        #deps = select_platform_target(
                #targets=['PerDriver'],
                #not_platform_targets=['//Platforms/Common:PerDriver']
               #),
#)
